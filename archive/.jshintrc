{
    // enforcing options
    "indent"        : 2,     // force tab-width - removed soon :(
    "curly"         : true,  // force curly braces
    "eqeqeq"        : true,  // force ===
    "freeze"        : true,  // prevent modifying built in objets
    "latedef"       : true,  // prevent use of vars before their defs
    "newcap"        : true,  // prevent uncapitalized constructors - removed soon :(
    "noarg"         : true,  // prevent arguments.callee and arguments.caller
    "nonbsp"        : true,  // prevent mac option-space characters
    "plusplus"      : true,  // prevent ++ usage
    "undef"         : true,  // prevent accessing undeclared vars
    "unused"        : true,  // prevent having unused vars
    "trailing"      : true,  // prevent trailing whitespace
    "singleGroups"  : true,  // prevent unnecessary grouping operators

    // complexity
    "maxparams"     : 5,     // max params per function
    "maxdepth"      : 5,     // max levels of nesting
    "maxstatements" : 50,    // max statements per fn
    "maxcomplexity" : 11,    // max cyclomatic complexity
    "maxlen"        : 85,    // max line length

    // relaxing opts
    "esnext"        : true,  // allow harmony syntax (const in partic.)
    "eqnull"        : true,  // allow == null to check for undef or null
    "laxcomma"      : true,  // allow comma-first style
    "validthis"     : true,  // allow using this in non-constructor function

    // legacy + misc
    "esnext"        : true,  // allow ES6 now
    "node"          : true,  // we need commonjs globals
    "browser"       : true,  // we need browser global
    "strict"        : false, // dont want "use strict" everywhere..
    "white"         : false  // dont want awful crockford whitespace style
}
