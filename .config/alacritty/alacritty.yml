font:
  size: 16

shell:
  program: /opt/homebrew/bin/bash
  # start zellij with a small sleep to ensure it gets right dimensions
  args:
   - -c
   - sleep 0.2 && /opt/homebrew/bin/zellij

window:
  startup_mode: Maximized
  decorations: buttonless
  opacity: 0.8
